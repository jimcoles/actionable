package com.oculussoftware.api.repi;

import com.oculussoftware.api.sysi.*;
import java.util.*;

/** An IRSet is collection that does not allow duplicates. */
public interface IRSet extends IRCollection
{
  
}